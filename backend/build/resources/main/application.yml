server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hiddengrowth?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: hg
    password: hgpass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

ai:
  base-url: ${AI_BASE_URL:http://localhost:8000}

analysis:
  worker:
    fixedDelayMs: 3000
